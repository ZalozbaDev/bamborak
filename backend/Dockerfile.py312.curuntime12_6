# docker build -f Dockerfile.py312.curuntime12_6 --progress=plain -t bamborak_backend_cuda_minimal .

# rebuild from scratch
# <... same cmdline ...>  -t bamborak_backend_cuda_minimal . --no-cache



# ==============================
# Stage 1 – Builder
# ==============================
FROM nvidia/cuda:12.6.2-devel-ubuntu24.04 AS bamborak_cuda_builder

ENV DEBIAN_FRONTEND=noninteractive
WORKDIR /opt/build

RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 python3-venv python3-pip \
    git pkgconf \
    libavformat-dev libavcodec-dev libavdevice-dev \
    libavutil-dev libavfilter-dev libswscale-dev libswresample-dev \
    ffmpeg \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# venv außerhalb vom App-Ordner (bessere Cachebarkeit)
RUN python3 -m venv /opt/venv
ENV PATH="/opt/venv/bin:$PATH"

# ---- Python Dependencies zuerst kopieren (Cache!)
COPY requirements_voice_changer.txt /

RUN pip install --upgrade pip setuptools wheel

RUN pip install coqui-tts==0.27.2 Flask==3.1.2 flask-cors==6.0.1
RUN pip install -r /requirements_voice_changer.txt

RUN pip install torch torchvision torchaudio \
    --index-url https://download.pytorch.org/whl/cu126

# ---- OpenVoice separat (Cache bei Git SHA fix)
RUN git clone https://github.com/ZalozbaDev/OpenVoice /opt/OpenVoice \
    && cd /opt/OpenVoice \
    && git checkout c74f274e32cedb48479500d59aacf57b757541bb \
    && pip install -e .

# ==============================
# Stage 2 – Runtime (minimal!)
# ==============================
FROM nvidia/cuda:12.6.2-runtime-ubuntu24.04

LABEL com.nvidia.cuda.version="12.6"
LABEL license="Includes NVIDIA CUDA redistributables subject to NVIDIA EULA"

ENV DEBIAN_FRONTEND=noninteractive
WORKDIR /usr/app/src

# nur Runtime-Libraries (KEINE dev!)
RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 \
    libsndfile1 sox libsox-fmt-all \
    ffmpeg \
    && rm -rf /var/lib/apt/lists/*

# venv vom Builder übernehmen
COPY --from=bamborak_cuda_builder /opt/venv /opt/venv
COPY --from=bamborak_cuda_builder /opt/OpenVoice /opt/OpenVoice
ENV PATH="/opt/venv/bin:$PATH"

# App-Code zuletzt kopieren → C++ Rebuild nur bei Codeänderung
COPY . .

RUN mkdir -p logs temp tts_models voice_changer

EXPOSE 8080

RUN chmod 755 start.sh

CMD ["./start.sh", "logs"]
